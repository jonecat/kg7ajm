# Build output: regenerated by `bundle exec jekyll build` and by CI.
_site/
.jekyll-cache/
.jekyll-metadata

# Local gem install (bundle install --path). CI installs its own.
vendor/bundle/

# macOS
.DS_Store
